services:
  nginx:
      build:
        context: requirements/nginx
        dockerfile: Dockerfile
      image: nginx
      container_name: nginx
      restart: unless-stopped
      working_dir: /var/www
      ports:
        - "443:443"
        # - "80:80"
#       networks:
#         inception_net
# networks:
#    inception_net:
#        driver: bridge